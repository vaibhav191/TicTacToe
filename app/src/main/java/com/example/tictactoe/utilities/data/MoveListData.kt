package com.example.tictactoe.utilities.data

// Data class holding a list of moves and their corresponding states
// Used for representing states available for a player and the board
data class MoveListData(val moves: MutableList<MoveStateData>) {
}
